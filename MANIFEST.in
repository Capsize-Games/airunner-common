# The sdist carries its own README so the sdist -> wheel phase of
# `python -m build` can resolve the long_description (issue #2061). In a
# checkout, package_metadata.README resolves the repo-root README the
# same way (parents[0] of <repo>/airunner_common/package_metadata.py).
include README.md
